name: Test workflow
run-name: "Ref: ${{ github.action_ref }}"
on: 
  - workflow_dispatch
  - push
jobs:
  test_job:
    name: "Committed with following SHA: ${{ github.sha }}"
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: echo
        run: echo Hello, World!

      - run: ls -la
        
    
